{% extends "base/dashboard_base.html" %}
{% load static i18n humanize %}

{% block title %}{% trans "Student Dashboard" %} - Zumodra{% endblock %}

{% block breadcrumb %}
<li class="flex items-center">
    <svg class="flex-shrink-0 w-5 h-5 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
    </svg>
    <span class="ml-2 text-sm font-medium text-gray-500 dark:text-gray-400">{% trans "Student Dashboard" %}</span>
</li>
{% endblock %}

{% block page_title %}{% trans "Student Dashboard" %}{% endblock %}

{% block page_description %}
<p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
    {% trans "Track your co-op terms, applications, and career progress." %}
</p>
{% endblock %}

{% block page_actions %}
<a href="{% url 'careers:job_list' %}?type=coop"
   class="inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-lg text-white bg-primary-600 hover:bg-primary-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500 transition-colors">
    <svg class="w-5 h-5 mr-2 -ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
    </svg>
    {% trans "Find Co-op Jobs" %}
</a>
{% endblock %}

{% block content %}
<div class="space-y-6">
    <!-- Profile Summary Card -->
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm overflow-hidden">
        <div class="p-6">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-6">
                <!-- Student Info -->
                <div class="flex items-center gap-4">
                    {% if user.profile.avatar %}
                    <img src="{{ user.profile.avatar.url }}" alt="{{ user.get_full_name }}"
                         class="w-16 h-16 rounded-full object-cover ring-4 ring-gray-100 dark:ring-gray-700">
                    {% else %}
                    <div class="w-16 h-16 rounded-full bg-primary-100 dark:bg-primary-900/30 flex items-center justify-center ring-4 ring-gray-100 dark:ring-gray-700">
                        <span class="text-2xl font-medium text-primary-600 dark:text-primary-400">
                            {{ user.get_full_name|slice:":1"|upper }}
                        </span>
                    </div>
                    {% endif %}
                    <div>
                        <h2 class="text-xl font-bold text-gray-900 dark:text-white">{{ user.get_full_name }}</h2>
                        <p class="text-sm text-gray-500 dark:text-gray-400">{{ student_profile.program_name }}</p>
                        <p class="text-sm text-gray-500 dark:text-gray-400">{{ student_profile.institution_name }}</p>
                    </div>
                </div>

                <!-- Quick Stats -->
                <div class="flex flex-wrap gap-4">
                    <div class="text-center px-4">
                        <p class="text-2xl font-bold text-primary-600 dark:text-primary-400">{{ student_profile.work_terms_completed }}</p>
                        <p class="text-xs text-gray-500 dark:text-gray-400">{% trans "Terms Done" %}</p>
                    </div>
                    <div class="text-center px-4 border-l border-gray-200 dark:border-gray-700">
                        <p class="text-2xl font-bold text-gray-900 dark:text-white">{{ student_profile.work_terms_required }}</p>
                        <p class="text-xs text-gray-500 dark:text-gray-400">{% trans "Required" %}</p>
                    </div>
                    <div class="text-center px-4 border-l border-gray-200 dark:border-gray-700">
                        <p class="text-2xl font-bold text-gray-900 dark:text-white">Year {{ student_profile.current_year|default:"?" }}</p>
                        <p class="text-xs text-gray-500 dark:text-gray-400">{% trans "Current" %}</p>
                    </div>
                </div>
            </div>

            <!-- Enrollment Status -->
            <div class="mt-6 pt-6 border-t border-gray-200 dark:border-gray-700 flex flex-wrap items-center gap-3">
                <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium
                    {% if student_profile.enrollment_status == 'active' %}bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-300
                    {% elif student_profile.enrollment_status == 'on_coop' %}bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300
                    {% elif student_profile.enrollment_status == 'graduated' %}bg-purple-100 text-purple-800 dark:bg-purple-900/30 dark:text-purple-300
                    {% else %}bg-gray-100 text-gray-800 dark:bg-gray-700 dark:text-gray-300{% endif %}">
                    {{ student_profile.get_enrollment_status_display }}
                </span>
                {% if student_profile.enrollment_verified %}
                <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400">
                    <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                    </svg>
                    {% trans "Verified" %}
                </span>
                {% endif %}
                <span class="text-sm text-gray-500 dark:text-gray-400">
                    {{ student_profile.get_student_type_display }} &bull; {{ student_profile.get_program_type_display }}
                </span>
            </div>
        </div>
    </div>

    <!-- Active/Upcoming Term -->
    {% if active_term %}
    <div class="bg-gradient-to-r from-primary-600 to-primary-700 rounded-xl shadow-sm text-white p-6">
        <div class="flex items-start justify-between">
            <div>
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-white/20 text-white mb-2">
                    {% trans "Current Term" %}
                </span>
                <h3 class="text-xl font-bold">{{ active_term.job_title }}</h3>
                <p class="text-primary-100 mt-1">{{ active_term.employer_name }}</p>
            </div>
            <a href="{% url 'frontend:accounts:coop_term_detail' active_term.uuid %}"
               class="px-4 py-2 bg-white/10 hover:bg-white/20 rounded-lg text-sm font-medium transition-colors">
                {% trans "View Details" %}
            </a>
        </div>
        <div class="mt-6 grid grid-cols-2 md:grid-cols-4 gap-4">
            <div>
                <p class="text-xs text-primary-200">{% trans "Start Date" %}</p>
                <p class="font-medium">{{ active_term.start_date|date:"M d, Y" }}</p>
            </div>
            <div>
                <p class="text-xs text-primary-200">{% trans "End Date" %}</p>
                <p class="font-medium">{{ active_term.end_date|date:"M d, Y" }}</p>
            </div>
            <div>
                <p class="text-xs text-primary-200">{% trans "Location" %}</p>
                <p class="font-medium">{{ active_term.location|default:"Remote" }}</p>
            </div>
            <div>
                <p class="text-xs text-primary-200">{% trans "Compensation" %}</p>
                <p class="font-medium">${{ active_term.hourly_rate|floatformat:2 }}/hr {{ active_term.currency }}</p>
            </div>
        </div>
    </div>
    {% elif upcoming_term %}
    <div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-xl p-6">
        <div class="flex items-start gap-4">
            <div class="flex-shrink-0">
                <svg class="w-8 h-8 text-blue-600 dark:text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
            </div>
            <div class="flex-1">
                <h3 class="text-lg font-semibold text-blue-900 dark:text-blue-100">{% trans "Upcoming Co-op Term" %}</h3>
                <p class="text-sm text-blue-700 dark:text-blue-300 mt-1">
                    {% if upcoming_term.employer_name %}
                    {{ upcoming_term.job_title }} at {{ upcoming_term.employer_name }}
                    {% else %}
                    {% trans "Start Date:" %} {{ upcoming_term.start_date|date:"M d, Y" }}
                    {% endif %}
                </p>
                <div class="mt-4 flex items-center gap-3">
                    {% if upcoming_term.status == 'searching' %}
                    <a href="{% url 'careers:job_list' %}?type=coop"
                       class="inline-flex items-center px-4 py-2 text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 rounded-lg transition-colors">
                        {% trans "Search Jobs" %}
                    </a>
                    {% else %}
                    <a href="{% url 'frontend:accounts:coop_term_detail' upcoming_term.uuid %}"
                       class="inline-flex items-center px-4 py-2 text-sm font-medium text-blue-700 bg-white hover:bg-blue-50 rounded-lg border border-blue-300 transition-colors">
                        {% trans "View Details" %}
                    </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
    {% else %}
    <div class="bg-yellow-50 dark:bg-yellow-900/20 border border-yellow-200 dark:border-yellow-800 rounded-xl p-6">
        <div class="flex items-center gap-4">
            <svg class="w-8 h-8 text-yellow-600 dark:text-yellow-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
            </svg>
            <div>
                <h3 class="font-medium text-yellow-800 dark:text-yellow-200">{% trans "No Active or Upcoming Terms" %}</h3>
                <p class="text-sm text-yellow-700 dark:text-yellow-300">{% trans "You don't have any co-op terms scheduled. Start searching for opportunities!" %}</p>
            </div>
        </div>
    </div>
    {% endif %}

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Past Terms -->
        <div class="lg:col-span-2">
            <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm overflow-hidden">
                <div class="px-6 py-4 border-b border-gray-200 dark:border-gray-700 flex items-center justify-between">
                    <h3 class="text-lg font-semibold text-gray-900 dark:text-white">{% trans "Work Term History" %}</h3>
                    <a href="{% url 'frontend:accounts:coop_terms_list' %}"
                       class="text-sm text-primary-600 dark:text-primary-400 hover:underline">
                        {% trans "View All" %}
                    </a>
                </div>

                {% if past_terms %}
                <div class="divide-y divide-gray-200 dark:divide-gray-700">
                    {% for term in past_terms %}
                    <div class="px-6 py-4 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors">
                        <div class="flex items-start justify-between">
                            <div>
                                <div class="flex items-center gap-2">
                                    <h4 class="text-sm font-medium text-gray-900 dark:text-white">{{ term.job_title }}</h4>
                                    <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium
                                        {% if term.status == 'completed' %}bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-300
                                        {% elif term.status == 'cancelled' %}bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-300
                                        {% else %}bg-gray-100 text-gray-800 dark:bg-gray-700 dark:text-gray-300{% endif %}">
                                        {{ term.get_status_display }}
                                    </span>
                                </div>
                                <p class="text-sm text-gray-500 dark:text-gray-400">{{ term.employer_name }}</p>
                                <p class="text-xs text-gray-400 dark:text-gray-500 mt-1">
                                    {{ term.start_date|date:"M Y" }} - {{ term.end_date|date:"M Y" }}
                                </p>
                            </div>
                            <div class="text-right">
                                {% if term.employer_rating %}
                                <div class="flex items-center gap-1">
                                    <svg class="w-4 h-4 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                                    </svg>
                                    <span class="text-sm font-medium text-gray-900 dark:text-white">{{ term.employer_rating }}</span>
                                </div>
                                <p class="text-xs text-gray-500">{% trans "Your Rating" %}</p>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
                {% else %}
                <div class="px-6 py-12 text-center">
                    <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                    </svg>
                    <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">{% trans "No completed work terms yet." %}</p>
                </div>
                {% endif %}
            </div>
        </div>

        <!-- Sidebar -->
        <div class="space-y-6">
            <!-- Pending Applications -->
            <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm overflow-hidden">
                <div class="px-6 py-4 border-b border-gray-200 dark:border-gray-700">
                    <h3 class="text-lg font-semibold text-gray-900 dark:text-white">{% trans "My Applications" %}</h3>
                </div>
                {% if pending_applications %}
                <ul class="divide-y divide-gray-200 dark:divide-gray-700">
                    {% for app in pending_applications|slice:":5" %}
                    <li class="px-6 py-3">
                        <a href="{% url 'careers:application_detail' app.id %}" class="block hover:bg-gray-50 dark:hover:bg-gray-700/50 -mx-6 -my-3 px-6 py-3">
                            <p class="text-sm font-medium text-gray-900 dark:text-white truncate">{{ app.job.title }}</p>
                            <p class="text-xs text-gray-500 dark:text-gray-400">{{ app.job.company_name }}</p>
                            <div class="mt-1">
                                <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium
                                    {% if app.status == 'under_review' %}bg-yellow-100 text-yellow-800
                                    {% elif app.status == 'interview' %}bg-blue-100 text-blue-800
                                    {% else %}bg-gray-100 text-gray-800{% endif %}">
                                    {{ app.get_status_display }}
                                </span>
                            </div>
                        </a>
                    </li>
                    {% endfor %}
                </ul>
                {% else %}
                <div class="px-6 py-8 text-center">
                    <p class="text-sm text-gray-500 dark:text-gray-400">{% trans "No pending applications." %}</p>
                </div>
                {% endif %}
            </div>

            <!-- Quick Actions -->
            <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm p-6">
                <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">{% trans "Quick Actions" %}</h3>
                <div class="space-y-3">
                    <a href="{% url 'frontend:accounts:cv_list' %}"
                       class="flex items-center gap-3 p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors">
                        <div class="flex-shrink-0 w-10 h-10 rounded-lg bg-primary-100 dark:bg-primary-900/30 flex items-center justify-center">
                            <svg class="w-5 h-5 text-primary-600 dark:text-primary-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                            </svg>
                        </div>
                        <div>
                            <p class="text-sm font-medium text-gray-900 dark:text-white">{% trans "Manage CVs" %}</p>
                            <p class="text-xs text-gray-500">{% trans "Update your resumes" %}</p>
                        </div>
                    </a>
                    <a href="{% url 'frontend:accounts:student_profile_edit' %}"
                       class="flex items-center gap-3 p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors">
                        <div class="flex-shrink-0 w-10 h-10 rounded-lg bg-blue-100 dark:bg-blue-900/30 flex items-center justify-center">
                            <svg class="w-5 h-5 text-blue-600 dark:text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                            </svg>
                        </div>
                        <div>
                            <p class="text-sm font-medium text-gray-900 dark:text-white">{% trans "Edit Profile" %}</p>
                            <p class="text-xs text-gray-500">{% trans "Update your info" %}</p>
                        </div>
                    </a>
                    <a href="{% url 'frontend:accounts:verification-dashboard' %}"
                       class="flex items-center gap-3 p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors">
                        <div class="flex-shrink-0 w-10 h-10 rounded-lg bg-green-100 dark:bg-green-900/30 flex items-center justify-center">
                            <svg class="w-5 h-5 text-green-600 dark:text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
                            </svg>
                        </div>
                        <div>
                            <p class="text-sm font-medium text-gray-900 dark:text-white">{% trans "Verifications" %}</p>
                            <p class="text-xs text-gray-500">{% trans "Verify your credentials" %}</p>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
